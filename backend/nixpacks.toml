# Nixpacks — configuración de build para Railway
# Solo pineamos Python 3.11 y las libs de sistema para psycopg2.
# El install phase lo maneja nixpacks automáticamente desde requirements.txt.

[phases.setup]
nixPkgs = ["python311", "postgresql_15"]
